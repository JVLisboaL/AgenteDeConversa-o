import requests
import json
import logging
from .config import BASE_URL, PARAMS, HEADERS  # Importa do arquivo config.py

def gerar_resposta(mensagem, personalidade, historico):
    # ... (código da função gerar_resposta)
    pass
